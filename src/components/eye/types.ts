export interface IInnerPartProps {
    transitionStyle: React.CSSProperties;
    groupProps: {
        className: string;
        style: {
            transition: string;
        };
        transform: string;
    };
}
