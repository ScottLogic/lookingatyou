import { getSelections } from '../../store/selectors/detectionSelectors';

describe('getSelections', () => {
    const detections = [];
    const previousTargets = { x: 0, y: 0 };
    const previousColours = { r: 0, g: 0, b: 0 };
    const imageData = new ImageData(10, 10);
});
