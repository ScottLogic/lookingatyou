version: 2
jobs:

  test:
    docker:
      - image: 'circleci/node:10.16'
    steps:
      - checkout
      - restore_cache:
          key: lookingatyou-{{ checksum "package-lock.json" }}
      - run:
          name: Install npm dependencies
          command: npm install
      - save_cache:
          key: lookingatyou-{{ checksum "package-lock.json" }}
          paths:
            - node_modules
      - run:
          name: Run tests
          command: npm run test

  deploy:
    docker:
      - image: 'circleci/python:3.6.4-stretch-node'
    steps:
      - checkout
      - run: sudo pip install awscli --upgrade
      - run: aws --version
      - run: npm --version
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Build project
          command: npm run build
      - run:
          name: "Deploy to S3"
          command: aws s3 sync ./build/ s3://looking-at-you/ --delete

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - test
      - deploy:
          filters:
            branches:
              only: master
          requires:
            - test
